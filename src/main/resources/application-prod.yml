spring:
  jpa:
    hibernate:
      ddl-auto: validate
    show-sql: false

server:
  port: 8080

app:
  cookie:
    # 운영: 프론트/백을 같은 도메인(collab.p-e.kr) 경로분리로 서빙 → same-origin 이므로 Lax 로 충분.
    # HTTPS 이므로 secure=true (평문 HTTP였다면 secure=false 로 내려야 쿠키가 붙음).
    same-site: Lax
    secure: true
