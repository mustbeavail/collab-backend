# Maven
target/
*.jar
*.war

# IDE
.vscode/
.idea/
*.iml

# Logs
logs/
*.log

# OS
.DS_Store

# Local config (sensitive values - never commit)
application-local.yml
*.local.yml
.env

# User-uploaded files (runtime, not source)
uploads/

# Local static ffmpeg binary (운영은 Docker 이미지에 apt로 설치, 로컬 테스트용만 여기 배치)
tools/ffmpeg/
